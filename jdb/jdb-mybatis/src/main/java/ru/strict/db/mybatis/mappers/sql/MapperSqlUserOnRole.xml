<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ru.strict.db.mybatis.mappers.sql.MapperSqlUserOnRole">

    <resultMap id="userOnRole" type="ru.strict.db.core.entities.EntityUserOnRole">
        <id property="id" column="id"/>
        <result property="userId" column="userx_id"/>
        <result property="roleId" column="roleuser_id"/>
    </resultMap>

    <resultMap id="userOnRoleFill" type="ru.strict.db.core.entities.EntityUserOnRole" extends="userOnRole">
        <association property="user"
                     column="userx_id"
                     javaType="ru.strict.db.core.entities.EntityUser"
                     select="ru.strict.db.mybatis.mappers.sql.MapperSqlUser.read"
                     fetchType="lazy"/>
        <association property="role"
                     column="roleuser_id"
                     javaType="ru.strict.db.core.entities.EntityRoleuser"
                     select="ru.strict.db.mybatis.mappers.sql.MapperSqlRoleuser.read"
                     fetchType="lazy"/>
    </resultMap>

    <insert id="create" parameterType="ru.strict.db.core.entities.EntityUserOnRole">
        INSERT INTO user_on_role (id, userx_id, roleuser_id) VALUES (#{id}, #{userId}, #{roleId})
    </insert>

    <select id="read" parameterType="java.lang.Object" resultMap="userOnRole">
        SELECT * FROM user_on_role WHERE id = #{id}
    </select>

    <select id="readAll" parameterType="String" resultMap="userOnRole">
        SELECT * FROM user_on_role ${value}
    </select>

    <update id="update" parameterType="ru.strict.db.core.entities.EntityUserOnRole">
        UPDATE user_on_role SET userx_id = #{userId}, roleuser_id = #{roleId} WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Object">
        DELETE FROM user_on_role WHERE id = #{id}
    </delete>

    <select id="readFill" parameterType="java.lang.Object" resultMap="userOnRoleFill" >
        SELECT * FROM user_on_role WHERE id = #{id}
    </select>

    <select id="readAllFill" parameterType="String" resultMap="userOnRoleFill">
        SELECT * FROM user_on_role ${value}
    </select>

    <select id="readCount" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM user_on_role ${value}
    </select>
</mapper>